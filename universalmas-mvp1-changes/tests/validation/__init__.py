"""
Validation Tests Package
========================

Contains validation tests that ensure Codex implementations match interface contracts.
These tests must pass before subsequent phases can begin.

Test Structure:
- test_config_completeness.py: Validates WorkflowConfig implementation (Phase 1 gate)
- test_interface_compliance.py: Validates Redis implementations (Phase 2 gate)
"""
